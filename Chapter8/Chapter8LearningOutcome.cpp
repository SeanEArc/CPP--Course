#include <iostream>
#include <string>

using namespace std;


int main() {

      /* THIS IS THE CODE FOR 31) The arrays array1 and array2 each hold 25 integer elements. Write code that copies the values in array1 to array2.

      int array1[25];
      int array2[25];

      for (int i = 0; i < 25; i++) {
            array2[i] = array1[1];

      }

      return 0;

      */


      // 33) I COULD NOT LOCATE THE "pop.dat" FILE MENTION FOR THIS PROBLEM WITHIN THE BOOK SO THIS IS ASSUMED THAT THE COUNTRY AND POPULATION HAS ITEMS WITHIN IT'S ARRAYS.
      //For students who have covered Chapter 7: In a program you need to store the names and populations of 12 countries. Create an appropriate array to store this information and then write the code needed to read the information into the array from a file named pop.dat.

/*
      string country[12];
      double population[12];

      for (int i = 0; i < 12; i++) {
            cout << "Country: " << country[i] << " Population: " << population[i] << endl;
      }
*/



      // 34) A weather analysis program uses the following array to store the temperature for each hour of the day on each day of a week.

      // int temp[7][24];


      // A) Finding Tuesday's average temperature
      // int totalTuesdayTemp = 0;
      // for (int i = 0; i < 24; i++) {
      //       totalTuesdayTemp += temp[2][i];
      // }
      // double tuesdayAvgTemp = totalTuesdayTemp / 24.0;
      // cout << "Tuesday's Average Temperature: " << tuesdayAvgTemp << endl;
      
      // B) Finiding the averavage weekly noon temperature
      // int totalNoonTemp = 0;
      // for (int i = 0; i < 7; i++) {
      //       totalNoonTemp += temp[i][12];
      // }

      // double averageNoonTemp = totalNoonTemp / 7.0;


      // 35) In a program you need to store the identification numbers of 10 employees (as ints) and their weekly gross pay (as doubles). Define two arrays that may be used in parallel to store the 10 employee identification numbers and 10 weekly gross pay amounts. Write a loop that uses these arrays to print each employee’s identification number and weekly gross pay.

      // int size = 10;
      // int empID[10];
      // double grossPay[10];

      // for(int i = 0; i < size; i++) {
      //       cout << "Enter Employee ID for employee " << (i + 1) << ": ";
      //       cin >> empID[i];
      //       cout << "Enter their gross pay: ";
      //       cin >> grossPay[i];

      // }

      // for (int i = 0; i < size; i++) {
      //       cout << "Employee ID: " << empID[i] << " Gross Pay: $" << grossPay[i] << endl;
      // }



}